theme color: #1a7f72

# Backend:
-

# Frontend:
src/
├── App.vue                      # Shell: sidebar + <router-view>
├── main.js                      # App entry, mounts router
├── router/index.js              # All 7 routes, lazy-loaded except Dashboard
├── components/
│   ├── AppSidebar.vue           # Left nav, network status pill, inline SVG icons
│   ├── StatCard.vue             # Reusable stat card with slot for icon + extra
│   └── NodeRow.vue              # Table row with sparkline, status dot, temp
└── views/
    ├── DashboardView.vue        # Full dashboard: stats, bar chart, alerts, node table
    └── {Nodes,History,Alerts,Map,Settings,Docs}View.vue  # Placeholder stubs
